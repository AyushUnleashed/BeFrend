<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <ImageView
        android:src="@drawable/img_user_profile_sample"
        android:id="@+id/imgViewUserProfile"
        android:scaleType="centerCrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <ImageView
        android:id="@+id/bg_profile_bottom"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:alpha="0.5"
        android:src="@drawable/item_bottom_bg"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />

    <TextView
        app:layout_constraintLeft_toLeftOf="@id/bg_profile_bottom"
        app:layout_constraintTop_toTopOf="@id/bg_profile_bottom"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:id="@+id/tvUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:fontFamily="@font/poppins_bold"
        android:text="Ayush Yadav"
        android:textColor="@color/white" />

    <TextView
        app:layout_constraintLeft_toLeftOf="@id/bg_profile_bottom"
        app:layout_constraintRight_toRightOf="@id/bg_profile_bottom"
        app:layout_constraintTop_toBottomOf="@id/tvUserName"
        android:id="@+id/tvUserBio"
        android:layout_marginStart="20dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:fontFamily="@font/poppins_regular"
        android:text="Interested in Software Developement, I Try to Expose Myself to new Things, I like MultiLingual Songs btw."
        android:textColor="@color/white" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>